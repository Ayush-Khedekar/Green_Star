import type { ReactNode } from "react";

export interface ServiceType {
  Icons: ReactNode;
  Title: string;
}

const OurService = ({}: { ServiceProp: ServiceType }) => {
  return (
    <></>
  );
};

export default OurService;
